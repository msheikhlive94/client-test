# ──────────────────────────────────────────
# ProjoFlow Environment Variables
# Copy this file to .env.local and fill in your values
# ──────────────────────────────────────────

# ──── Supabase (Required) ────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ──── License Validation (Optional) ────
# Central license validation API endpoint
# Default: https://www.projoflow.com/api/license/validate
# Only change this if ProjoFlow provides a custom validation URL
# NEXT_PUBLIC_LICENSE_API_URL=https://www.projoflow.com/api/license/validate

# ──── Email (Optional — for client invitations) ────
# Get your API key from resend.com
RESEND_API_KEY=re_your_resend_api_key_here

# ──── License System (Required) ────
# Admin secret for generating licenses manually
# Generate a secure random string (e.g., openssl rand -hex 32)
ADMIN_SECRET=your_secure_admin_secret_here

# Supabase service role key (for license management)
# Find in: Supabase Dashboard → Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Optional: Webhook secrets for payment platforms
LEMONSQUEEZY_WEBHOOK_SECRET=your_lemonsqueezy_webhook_secret
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# ──── Landing Page (Optional — defaults shown) ────
# NEXT_PUBLIC_PRICE=$197
# NEXT_PUBLIC_ORIGINAL_PRICE=$497
# NEXT_PUBLIC_PURCHASE_URL=https://your-checkout-link.com
# NEXT_PUBLIC_DEMO_URL=https://demo.yoursite.com

# ──── Node Environment ────
NODE_ENV=development

# ──────────────────────────────────────────
# Setup Instructions:
# 1. Create a Supabase project at supabase.com
# 2. Copy your project URL and anon key from Settings → API
# 3. Run database migrations (see README.md)
# 4. (Optional) Sign up for Resend for email invitations
# 5. Start dev server: npm run dev
# ──────────────────────────────────────────
